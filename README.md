# process-monitoring-script
A script to monitor and manage critical processes like Apache, SSH, and MariaDB.
This is a simple bash script to monitor critical processes like Apache2, SSH, and MariaDB. It checks if the services are running and attempts to restart them if they are not. It also sends email alerts if a service goes down.

